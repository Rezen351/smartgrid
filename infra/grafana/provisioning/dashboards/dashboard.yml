apiVersion: 1
providers:
  - name: SmartGrid Dashboards
    orgId: 1
    folder: SmartGrid
    type: file
    disable: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards